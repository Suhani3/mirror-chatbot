// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

let config = {
  botName:"MirrorBot",
  initialMessages: [
    createChatBotMessage("Hi, I am a mirror chatbot. Start by sending me a message!")],
};

export default config;
